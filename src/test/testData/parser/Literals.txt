HOCON File
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(HoconToken.UNQUOTED_CHARS)('a')
        PsiWhiteSpace(' ')
        PsiElement(HoconToken.EQUALS)('=')
        PsiWhiteSpace(' ')
        HBoolean(BOOLEAN)
          PsiElement(HoconToken.UNQUOTED_CHARS)('true')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(HoconToken.UNQUOTED_CHARS)('b')
        PsiWhiteSpace(' ')
        PsiElement(HoconToken.EQUALS)('=')
        PsiWhiteSpace(' ')
        HNull(NULL)
          PsiElement(HoconToken.UNQUOTED_CHARS)('null')
    PsiWhiteSpace('\n')
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(HoconToken.UNQUOTED_CHARS)('c')
        PsiWhiteSpace(' ')
        PsiElement(HoconToken.EQUALS)('=')
        PsiWhiteSpace(' ')
        HNumber(NUMBER)
          PsiElement(HoconToken.UNQUOTED_CHARS)('1')
          PsiElement(HoconToken.PERIOD)('.')
          PsiElement(HoconToken.UNQUOTED_CHARS)('5')