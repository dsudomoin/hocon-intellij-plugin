HOCON File
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HPrefixedField(PREFIXED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(HoconToken.UNQUOTED_CHARS)('a')
        PsiElement(HoconToken.PERIOD)('.')
        HPrefixedField(PREFIXED_FIELD)
          HFieldKey(FIELD_KEY)
            HKeyPart(KEY_PART)
              HUnquotedString(UNQUOTED_STRING)
                PsiElement(HoconToken.UNQUOTED_CHARS)('b')
          PsiElement(HoconToken.PERIOD)('.')
          HValuedField(VALUED_FIELD)
            HFieldKey(FIELD_KEY)
              HKeyPart(KEY_PART)
                HUnquotedString(UNQUOTED_STRING)
                  PsiElement(HoconToken.UNQUOTED_CHARS)('c')
            PsiWhiteSpace(' ')
            PsiElement(HoconToken.EQUALS)('=')
            PsiWhiteSpace(' ')
            HNumber(NUMBER)
              PsiElement(HoconToken.UNQUOTED_CHARS)('1')